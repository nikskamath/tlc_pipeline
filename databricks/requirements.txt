# ============================================================
# PINNED VERSIONS — used identically in your local .venv, in
# Dockerfile.airflow, and in .github/workflows/*.yml
#
# dbt itself is NOT in this file — see snowflake/requirements.txt
# header for the full explanation. FUSION_VERSION.txt holds the pin.
# ============================================================

# ── Airflow ──────────────────────────────────────────────
apache-airflow==2.9.3
apache-airflow-providers-databricks==6.8.0

# ── Databricks (used by Python loader + Jobs API trigger script) ──
databricks-sdk==0.28.0
databricks-sql-connector==3.0.0

# ── AI Agent — LangChain + Gemini (free) ──────────────────
langchain==0.2.16
langchain-core==0.2.41
langchain-google-genai==1.0.10

# Uncomment to swap to Claude API:
# langchain-anthropic==0.1.23
# anthropic==0.34.2

# Uncomment to swap to local Ollama:
# langchain-ollama==0.1.3

# ── Data / ML ────────────────────────────────────────────
pandas==2.2.2
pyarrow==17.0.0
numpy==1.26.4
requests==2.32.3

# ── Dev tools ────────────────────────────────────────────
python-dotenv==1.0.1
black==24.8.0
pytest==8.3.2
pre-commit==3.8.0
sqlfluff==3.1.0
sqlfluff-templater-dbt==3.1.0
